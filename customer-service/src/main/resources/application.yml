spring:
   application:
     name: customer-service
server:
  port: 8301
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
     defaultZone: http://localhost:8302/eureka
  instance:
     hostname: localhost


